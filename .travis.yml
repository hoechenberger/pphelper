language: python

python:
  - "2.6"
  - "2.7"
  - "3.3"
  - "3.4"

before_install:
  - sudo apt-get update
  - sudo apt-get install libatlas-base-dev gfortran

install:
  - pip install numpy
  - pip install matplotlib
  - pip install scipy
  - pip install pandas
  - pip install .

script: py.test
